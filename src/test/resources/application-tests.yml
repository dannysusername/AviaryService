spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/AviaryService
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: spoop
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        '[format_sql]': true
  # Security is disabled for tests by excluding or overriding security configuration in test profile